get_emotion.py loads pretrained emotion models from emotion/
generator.py builds and loads pretrained transformer  model from model_checkpoint/ and generates replies.
history.py saves and loads history conversations.
intent.py loads pretrained intent models from intent/
